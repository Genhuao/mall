# mall

> A Vue.js project

### 关于数据库的配置在Server/routes/goods.js中可以看到，将根目录下的数据库文件dumall-goods和dumall-users导入mongodb数据库即可。用户名 admin，密码123456。

## Build Setup

``` bash
1.npm install

#在根目录下运行Server目录下bin下的www文件
2.node Server/bin\www

3.npm run dev

4.在浏览器输入http://localhost:8080/

```


